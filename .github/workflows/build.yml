name: Galata Mobil APK Build

on:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4  # v4 Guncellendi

    - name: Setup Python
      uses: actions/setup-python@v5 # v5 Guncellendi
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt

    - name: Setup Flutter & Flet Build Tools
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.19.0'

    - name: Build APK
      run: flet build apk --verbose 

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4  # KRITIK DUZELTME BURADA (v4)
      with:
        name: galata-mobil-apk
        path: build/apk/app-release.apk
